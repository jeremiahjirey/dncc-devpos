# name: Build and Push Docker Image.

# on:
#   push:
#     branches: [main]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_TOKEN }}

#       - name: Build Docker Image
#         run: docker buildx build --platform linux/amd64 -t ${{ secrets.DOCKER_USERNAME }}/belajar-devops:latest . --push

name: Build and Push Docker Image

on:
  #pull_request
  push:
    branches: ["main"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # permissions:
    #   contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        # with:
        #   fetch-depth: 0

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      # Setup Buildx (Docker builder)
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # - name: Generate production version
      #   id: version
      #   run: |
      #     LATEST_TAG=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n 1)
      #     if [ -z "$LATEST_TAG" ]; then
      #       VERSION="v0.1.0"
      #     else
      #       TAG_NO_V="${LATEST_TAG#v}"
      #       IFS='.' read -r MAJOR MINOR PATCH <<< "$TAG_NO_V"
      #       VERSION="v${MAJOR}.${MINOR}.$((PATCH + 1))"
      #     fi
      #     echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
      #     echo "Generated production version: ${VERSION}"

      # - name: Create and push git tag
      #   run: |
      #     VERSION="${{ steps.version.outputs.version }}"
      #     if git ls-remote --exit-code --tags origin "refs/tags/${VERSION}" >/dev/null 2>&1; then
      #       echo "Tag ${VERSION} already exists on remote."
      #     else
      #       git config user.name "github-actions[bot]"
      #       git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      #       git tag "${VERSION}"
      #       git push origin "${VERSION}"
      #       echo "Pushed tag ${VERSION}"
      #     fi

      # Build & Push image
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/belajar-devops:latest
          # ${{ secrets.DOCKER_USERNAME }}/belajar-devops:${{ steps.version.outputs.version }}

      # # Trigger deployment Server
      # - name: Trigger deployment on server
      #   run: |
      #     curl -X GET http://76.13.23.156:3000/api/deploy/${{ secrets.DEPLOYMENT_SECRET }}
